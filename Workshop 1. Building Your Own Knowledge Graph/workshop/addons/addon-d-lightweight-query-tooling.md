# Addon D: Lightweight Query Tooling

Introduce simple repo-level queries before advanced retrieval.

Examples:

- list all notes with `status: ai-drafted`
- list all notes tagged `pkb`
- find all notes linking to a target concept

This keeps querying understandable before moving to embeddings or vector search.
